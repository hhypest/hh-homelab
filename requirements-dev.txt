# Зависимости для проверок: те же версии стоят в CI.
# Ничего из этого не нужно на NAS — Home Assistant и медиа-стек работают без них.
pyyaml>=6.0
jinja2>=3.1
python-liquid>=2.0
yamllint>=1.35
pytest>=8.0
ruff>=0.6
